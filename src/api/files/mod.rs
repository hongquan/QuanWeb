// API endpoint collection to manage remote files on bunny.net storage.
// The /api/files/browse/<path> endpoint is modeled after Bunny Storage API.
// The /api/files/browse/<path> DELETE endpoint is for deleting files.

pub mod routes;
pub mod views;
